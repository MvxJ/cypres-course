version: '3.7'
services:
  cypress_tests:
    image: cypress_test
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./dockerReports:/app/cypress/results
      - ./dockerReports:/app/mochawesome-report